Hai pushato un certo file, quindi e' su github

Lo rimuovi in locale, quindi su github c'e' ancora

Come lo togli da github?

Semplice: in locale

```
git rm file
git commit -m "file removed"
git push
```
